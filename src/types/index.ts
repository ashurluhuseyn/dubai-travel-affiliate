/** @deprecated Import types from `@/data` instead. Kept for backward compatibility. */
export type {
  DestinationExperience,
  DestinationFilters,
  DestinationStat,
  ExperienceBadge,
  FilterGroup,
  FilterOption,
  SortOption,
  StatIconKey,
} from "@/data/types";
